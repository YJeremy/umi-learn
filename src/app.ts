import {ResponseError} from 'umi-request';
export const request = {
    prefix:'/api',
    /* errorHander:(error:ResponseError) =>{

        //console.log(error);
        return null
    }, */
}
/**
 * umi的运行时配置，都在 src/app.ts 中，我们在这里配置 umi-request 的配置。
 * 配置了所有请求的 prefix
 */
